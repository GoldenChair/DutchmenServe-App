import 'package:flutter/material.dart';

class FavoritedPage extends StatelessWidget {
  @override
  Widget build(BuildContext ctxt) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Favorited Events'),
        backgroundColor: Colors.indigo[800],
      ),
      body: Column(),
    );
  }
}
