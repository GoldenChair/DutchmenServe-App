import 'package:flutter/material.dart';

class EventInfo extends StatelessWidget {
  @override
  Widget build(BuildContext ctxt) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Service Event'),
        backgroundColor: Colors.indigo[800],
      ),
      body: Column(),
    );
  }
}
